<source>
  @type rocketmq
  host rocketMQ服务的IP
  port rocketMQ服务的端口
  consumer_group rocketMQ服务的消费者组
  topics rocketMQ服务的消费Topic,可有多个,逗号分割
  source_key project_id
  source_value 10001
</source>


<match ***>
  @type copy

  <store>
    @type stdout
  </store>

</match>